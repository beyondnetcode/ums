{
  "name": "ums-monorepo-workspace",
  "version": "1.0.0",
  "description": "Enterprise Monorepo for User Management System (UMS) using .NET 10, React, SQL Server, and spec-driven AI strategy BMAD-METHOD",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev:all": "nx run-many -t dev run --parallel",
    "build:all": "nx run-many -t build --parallel",
    "docs:audit": "python ../.bmad-core/scripts/validate_docs_consistency.py"
  },
  "devDependencies": {
    "@commitlint/cli": "21.0.2",
    "@commitlint/config-conventional": "21.0.2",
    "@nx/js": "22.7.5",
    "ctx7": "0.5.1",
    "eslint-plugin-sonarjs": "4.0.3",
    "nx": "22.7.5",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "@vitejs/plugin-react": "5.2.0"
  },
  "overrides": {
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "@eslint/plugin-kit": "0.3.4",
    "axios": "1.18.0",
    "fast-uri": "3.1.4",
    "form-data": "4.0.6",
    "nx": {
      "tmp": "0.2.7",
      "brace-expansion": "5.0.7"
    },
    "storybook": {
      "esbuild": "0.28.1"
    }
  }
}
