apiVersion: v1
kind: Service
metadata:
  name: {{ include "ums-helm.fullname" . }}
  labels:
    {{- include "ums-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ums-helm.selectorLabels" . | nindent 4 }}
