apiVersion: apps/v1
kind: Deployment
metadata:
  name: ums-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ums-redis
  template:
    metadata:
      labels:
        app: ums-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: ums-redis
spec:
  type: ClusterIP
  selector:
    app: ums-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
